import 'package:flutter/material.dart';

class ActualitesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text("Contenu de la page des Actualités"),
      ),
    );
  }
}
